# Local only

The bridge defaults to:

```text
127.0.0.1:8787
```

Keep it that way unless you know exactly why you need LAN access.

Good:

```text
Telegram polling -> local bridge -> PokeClaw
```

Bad:

```text
Public internet -> bridge -> phone automation
```
