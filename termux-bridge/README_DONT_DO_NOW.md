# Do not do now

Not now:

```text
APK refactor
Local model tuning
Native Telegram integration inside APK
Public internet exposure
Root-only automation
```

Now:

```text
Prove Termux -> Intent -> PokeClaw works.
Then prove Telegram -> Termux -> PokeClaw works.
```
