# APK later boundary

This folder does not build or change the APK.

Prepared now:

```text
Termux control layer
Telegram polling layer
Local HTTP bridge
Start and test scripts
```

APK work later:

```text
Build PokeClaw APK
Add result callback sender
Improve in-app callback routing
Maybe add native Telegram control only if still needed
```

Keep this split:

```text
Termux = trigger/router
PokeClaw APK = visible phone agent
```
