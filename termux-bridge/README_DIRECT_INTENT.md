# Direct intent test

This is the most important first test:

```bash
./direct-am-test.sh "how much battery left"
```

It bypasses the HTTP bridge and Telegram.

If this fails, fix PokeClaw install/settings before anything else.
